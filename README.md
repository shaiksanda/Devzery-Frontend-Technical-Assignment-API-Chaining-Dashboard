# API Chaining Dashboard

This project is a responsive web application built with React.js and Tailwind CSS, demonstrating the ability to handle complex API interactions through chaining multiple API calls.

## Setup Instructions

1. **Clone the repository:**
   ```bash
git clone <repository-url>

cd <project-directory>

npm install

npm start


Approach
The application utilizes React functional components and hooks to manage state and lifecycle events.
It integrates with the JSONPlaceholder API to perform GET and POST requests, demonstrating API chaining.
Tailwind CSS is used for styling, ensuring a responsive design that adapts to various screen sizes.

Assumptions and Decisions
Assumptions:

Users have basic knowledge of React and API interactions.
The JSONPlaceholder API is used as a mock API for demonstration purposes.
Decisions:

Each major feature is encapsulated in its own component for modularity and reusability.
State management is centralized in the main App component to pass necessary data and functions to child components.
Completed Features
User selection from a list fetched via a GET request.
Creation of a new post linked to the selected user via a POST request.
Display of the created post details.
Automatic fetching and display of comments related to the created post.
Known Issues
There may be occasional delays in loading due to network conditions.
The design can be improved further for better aesthetics.


### Step 3: Zipping Your Project Files

**1. Navigate to Your Project Directory:**
   - Open your file explorer and go to the root folder of your React project.

**2. Select Files for Zipping:**
   - Select all the files and folders in the project directory (you can hold `Ctrl` or `Shift` to select multiple items).

**3. Create a Zip File:**
   - **Windows:**
     - Right-click on the selected files and choose `Send to > Compressed (zipped) folder`.
   - **macOS:**
     - Right-click (or Ctrl-click) on the selected files and choose `Compress Items`.

**4. Name the Zip File:**
   - Name the zip file appropriately, such as `api_chaining_dashboard.zip`.

### Step 4: Sharing the Files

**1. Upload Your Files:**
   - Upload the zipped file to a cloud storage service such as Google Drive, Dropbox, or any other file-sharing platform.
   - Ensure that you set the file permissions to allow others to view or download it.

**2. Share the Recording:**
   - If you recorded your application, upload the recording to a platform like YouTube, Google Drive, or any video-sharing site.
   - Make sure the video is set to public or has shareable links.

**3. Copy and Share Links:**
   - After uploading, copy the shareable link for both the zip file and the recording.
   - Prepare to submit both links as part of your assignment.

### Example Submission Message
When you’re ready to submit, you might write a message like this:

---

**Subject:** Submission of API Chaining Dashboard Project

Hello [Instructor's Name],

I have completed the API Chaining Dashboard project as per the assignment requirements. Below are the links for the zip file and the recording of the application:

- **Zip File:** [Link to api_chaining_dashboard.zip]
- **Application Recording:** [Link to recording]

Please let me know if you have any questions or need further information.

Thank you!

Best regards,  
Shubham P Patil

---

This comprehensive guide should help you successfully complete your assignment. If you have any questions or need further assistance with any specific step, feel free to ask!
